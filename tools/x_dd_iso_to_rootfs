#!/bin/bash

# ---------------------------------------------------------------------
cp rootfs.ext2.clean rootfs.ext2
sync

dcfldd if=./os.iso of=./rootfs.ext2 bs=512k
sync

# ---------------------------------------------------------------------
